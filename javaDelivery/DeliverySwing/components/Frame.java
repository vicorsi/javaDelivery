package components;

import javax.swing.*;
import java.awt.*;

public class Frame extends JFrame {
    public Frame(){
        setVisible(true);
        setSize(350, 650);
        setLayout(null);
        getContentPane().setBackground(Color.RED);
    }
}
